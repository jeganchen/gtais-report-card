表名	描述	建议同步字段 (SQL 形式)
SCHOOLS	学校信息	SELECT ID, SCHOOL_NUMBER, NAME, ABBREVIATION, DCID FROM SCHOOLS
TERMS	学年与学期	SELECT ID, SCHOOLID, NAME, ABBREVIATION, FIRSTDAY, LASTDAY, YEARID, ISYEARREC, DCID FROM TERMS
TEACHERS	教师/员工信息	SELECT ID, DCID, LASTFIRST, FIRST_NAME, LAST_NAME, EMAIL_ADDR, SCHOOLID, STAFFSTATUS FROM TEACHERS


表名	描述	建议同步字段 (SQL 形式)
STUDENTS	学生主表	SELECT ID, DCID, STUDENT_NUMBER, FIRST_NAME, LAST_NAME, MIDDLE_NAME, GENDER, GRADE_LEVEL, SCHOOLID, HOME_ROOM, ENROLL_STATUS, ENTRYDATE, EXITDATE, DOB, FAMILY_IDENT, STREET, CITY, HOME_PHONE, GUARDIANEMAIL FROM STUDENTS


表名	描述	建议同步字段 (SQL 形式)
COURSES	课程定义	SELECT ID, COURSE_NUMBER, COURSE_NAME, CREDIT_HOURS, DCID FROM COURSES
SECTIONS	课节(班级)	SELECT ID, DCID, COURSE_NUMBER, SECTION_NUMBER, TEACHER, TERMID, SCHOOLID, EXPRESSION FROM SECTIONS
CC	学生选课记录	SELECT ID, DCID, STUDENTID, COURSE_NUMBER, SECTIONID, TERMID, SCHOOLID, DATEENROLLED, DATELEFT FROM CC


表名	描述	建议同步字段 (SQL 形式)
STANDARDS	学科标准定义	SELECT ID, DCID, IDENTIFIER, NAME, DESCRIPTION, SUBJECTAREA, LISTORDER FROM STANDARDS
STOREDGRADES	传统期末成绩	SELECT ID, DCID, STUDENTID, SCHOOLID, TERMID, COURSE_NUMBER, SECTIONID, GRADE, PERCENT, GPA_POINTS, STORECODE FROM STOREDGRADES
ATTENDANCE	考勤明细	SELECT ID, DCID, STUDENTID, ATT_DATE, ATTENDANCE_CODEID, PERIODID, SECTIONID, SCHOOLID, YearID, ATT_MODE_CODE FROM ATTENDANCE
ATTENDANCE_CODE	考勤代码字典	SELECT ID, DCID,SCHOOLID, YEARID, ATT_CODE, DESCRIPTION, PRESENCE_STATUS_CD FROM ATTENDANCE_CODE

表名	描述	建议同步字段 (SQL 形式)
PERSON	个人基础信息	SELECT ID, DCID, FIRSTNAME, LASTNAME, MIDDLENAME, ISACTIVE FROM PERSON
STUDENTCONTACTASSOC	学生-联系人关联	SELECT STUDENTCONTACTASSOCID, STUDENTDCID, PERSONID, CONTACTPRIORITYORDER, CURRRELTYPECODESETID FROM STUDENTCONTACTASSOC
EMAILADDRESS	电子邮箱	SELECT EMAILADDRESSID, EMAILADDRESS FROM EMAILADDRESS
PERSONEMAILADDRESSASSOC	个人-邮箱关联	SELECT PERSONEMAILADDRESSASSOCID, PERSONID, EMAILADDRESSID, ISPRIMARYEMAILADDRESS FROM PERSONEMAILADDRESSASSOC
PHONENUMBER	电话号码	SELECT PHONENUMBERID, PHONENUMBER FROM PHONENUMBER
PERSONPHONENUMBERASSOC	个人-电话关联	SELECT PERSONPHONENUMBERASSOCID, PERSONID, PHONENUMBERID, ISPREFERRED FROM PERSONPHONENUMBERASSOC
